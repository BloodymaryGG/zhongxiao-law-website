# 北京中晓律师事务所 — 微信公众号运营手册

> 给 Codex（或任何接手的人）快速上手用。配合 `CODEX.md`（官网）一起读。
> 本手册覆盖：公众号文章从写作 → 排版 → 上传草稿箱 → 发布的完整流程。

## 0. 账号信息与安全（必读）

- 公众号：**北京中晓律师事务所**
- AppID：`wxa08f7a3a83619332`
- AppSecret：⚠️ **不在此文档中**，由交接人单独提供（微信公众平台 → 设置与开发 → 基本配置）
- **本 GitHub 仓库是公开的，任何密钥/Token 严禁提交进 git**（已通过 `.gitignore` 排除配置，注意别手动 `git add -f`）

本地密钥配置文件（不走 git，直接放在你本机）：

```bash
# 在项目目录下创建 scripts/.wechat-config.json（目录可自建）：
mkdir -p scripts
cat > scripts/.wechat-config.json << 'EOF'
{
  "appid": "wxa08f7a3a83619332",
  "secret": "在这里填交接人给的 AppSecret"
}
EOF
```

### ⚠️ IP 白名单（最容易踩的坑）

微信要求：调用接口的服务器 IP 必须在白名单内，否则报错 `errcode: 40164`（invalid ip）。

- 后台位置：微信公众平台 → 设置与开发 → 基本配置 → IP 白名单
- 每次**换网络环境**（换电脑、换 WiFi、开/关代理）后，用 `curl https://myip.ipip.net` 查当前公网 IP，如报 40164 就去后台更新白名单
- 白名单可填多个 IP，用逗号分隔

## 1. 发布流程（5 步）

### Step 1: 获取 access_token

```bash
curl "https://api.weixin.qq.com/cgi-bin/token?grant_type=client_credential&appid={APPID}&secret={SECRET}"
```

返回 `access_token`，后续步骤都要用（有效期 7200 秒）。

### Step 2: 上传图片

- **正文配图** → `POST https://api.weixin.qq.com/cgi-bin/media/uploadimg?access_token={TOKEN}`（multipart/form-data，返回微信图床 URL，正文 `<img>` 引用此 URL）
- **封面图** → `POST https://api.weixin.qq.com/cgi-bin/material/add_material?type=image&access_token={TOKEN}`（返回 `media_id`，供草稿箱设置封面）

### Step 3: 转换 HTML

Markdown/文稿 → 公众号专用 HTML，**必须遵守第 2 节排版规范**：
- 所有图片 `src` 替换为 Step 2 得到的微信图床 URL
- 所有样式**必须内联**，禁止 `<style>` 标签和 CSS class

### Step 4: 上传草稿箱

```bash
node scripts/upload-draft.js --html 文章.html --title "文章标题" --author "北京中晓律师事务所"
```

脚本自动读 `scripts/.wechat-config.json`，调 `POST /cgi-bin/draft/add` 提交草稿。

### Step 5: 人工确认封面并发布

草稿上传后**封面图需人工在后台设置**：
1. 登录微信公众平台 → 内容与互动 → 草稿箱
2. 打开草稿 → 设置封面（用 Step 2 上传的封面素材）→ 保存
3. 确认无误后点「发布」

## 2. 排版规范（公众号 HTML 标准）

### 全局规则

- 所有样式**必须内联**，禁止 `<style>` 标签和 CSS class
- 正文不含 `<h1>`（标题由公众号系统渲染）
- 不用 base64 图片；不用 `<span class="bold">` 模拟加粗（必须用 `<strong>`）
- 不用 `<div>` 模拟分割线（必须用 `<hr>`）

### 元素样式速查

| 元素 | 标签 | 内联样式 |
|------|------|----------|
| 摘要块 | `<section>` | `margin:10px 0 15px;padding:15px 20px;background:#f7f8fa;border-left:4px solid #0EA5E9;font-size:15px;color:#555;line-height:1.6;` |
| 正文段落 | `<p>` | `margin:3px 0;font-size:16px;line-height:1.75;color:#333;` |
| 二级标题 | `<h2>` | `font-size:20px;font-weight:bold;color:#111;margin-top:15px;margin-bottom:5px;padding-left:12px;border-left:4px solid #0EA5E9;` |
| 三级标题 | `<h3>` | `font-size:17px;font-weight:bold;color:#222;margin-top:12px;margin-bottom:3px;` |
| 引用块 | `<blockquote>` | `margin:3px 0;padding:12px 18px;background:#f7f8fa;border-left:4px solid #0EA5E9;color:#555;font-style:italic;` |
| 图片 | `<p>` 包裹 | 图片 `max-width:100%;display:block;margin:0 auto;border-radius:12px;`，外层 `text-align:center;` |
| 落款 | `<p>` | `margin-top:20px;padding-top:10px;border-top:1px solid #eee;font-size:14px;color:#999;text-align:center;line-height:2;` |

### 关键词颜色高亮（重要）

纯加粗不够突出，**核心关键词必须加颜色**：

| 用途 | 颜色 | 写法 |
|------|------|------|
| 核心观点/金句 | 品牌蓝 `#0EA5E9` | `<strong style="color:#0EA5E9;">关键句</strong>` |
| 重要数据/数字 | 橙色 `#f59e0b` | `<strong style="color:#f59e0b;">40%</strong>` |
| 警示/风险 | 红色 `#ef4444` | `<strong style="color:#ef4444;">警示内容</strong>` |
| 正面/增长 | 绿色 `#10b981` | `<strong style="color:#10b981;">增长数据</strong>` |
| 普通强调 | 默认黑色 | `<strong>普通强调</strong>` |

原则：每段最多高亮 1-2 处；金句用品牌蓝；数字用橙色；别在一句话里混用多种颜色。

### 落款（中晓专用，固定不变）

```html
<p style="margin-top:20px;padding-top:10px;border-top:1px solid #eee;font-size:14px;color:#999;text-align:center;line-height:2;">北京中晓律师事务所<br/>以法立心 · 向晓而行</p>
```

⚠️ 技能模板自带的落款是作者「有用AI」的，**必须替换成上面这版**。

## 3. 写作套路（踩坑总结）

1. **先确认写在哪**：官网文章页，还是公众号草稿箱——两套流程，别搞混
2. **字数**：纯文字 2000-3000 字才够分量
3. **内容类型**：优先专业视角/法律干货（新规解读、案例普法、实务问答），少写律所动态
4. **配图**：用 Unsplash 实拍图（商务/法律/城市类），不要用程序生成的示意图
5. **文风**：短句、口语化、去掉 AI 腔（避免"综上所述""首先其次最后"这类套话）
6. **标题**：具体、有信息量，能让人一眼知道讲什么
7. 官网文章需配套封面图（`articles/cover-*.png` 命名风格），公众号文章封面在后台设置

## 4. 常见问题

| 问题 | 原因与解决 |
|------|-----------|
| 报错 40164 invalid ip | IP 不在白名单，见第 0 节 |
| 报错 40001 invalid credential | access_token 过期或 secret 错误，重新获取 |
| 图片裂了 | `<img>` 的 src 必须用微信图床 URL（Step 2），不能是本地路径 |
| 样式乱 | 检查是否用了 `<style>` 或 class，公众号会过滤非内联样式 |
| 草稿箱找不到文章 | 确认脚本输出 success 且 media_id 非空，再看草稿箱（不是"发表记录"） |

## 5. 联系与归属

- 公众号后台账号：中晓律所注册的邮箱/管理员微信（找律所负责人要登录权限）
- 官网技术栈相关问题见 `CODEX.md`
